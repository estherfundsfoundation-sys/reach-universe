(()=>{const c=document.createElement('canvas');c.setAttribute('aria-hidden','true');Object.assign(c.style,{position:'fixed',inset:'0',width:'100%',height:'100%',zIndex:'0',pointerEvents:'none',opacity:'.72'});document.body.prepend(c);const x=c.getContext('2d');let dots=[];function resize(){c.width=innerWidth*devicePixelRatio;c.height=innerHeight*devicePixelRatio;x.setTransform(devicePixelRatio,0,0,devicePixelRatio,0,0);dots=Array.from({length:Math.min(95,Math.floor(innerWidth/14))},()=>({x:Math.random()*innerWidth,y:Math.random()*innerHeight,r:Math.random()*1.7+.25,v:Math.random()*.42+.08,h:Math.random()>.5?190:310}))}function draw(){x.clearRect(0,0,innerWidth,innerHeight);dots.forEach(p=>{p.y-=p.v;if(p.y<0){p.y=innerHeight+12;p.x=Math.random()*innerWidth}x.beginPath();x.fillStyle=`hsla(${p.h},100%,70%,.65)`;x.shadowBlur=12;x.shadowColor=x.fillStyle;x.arc(p.x,p.y,p.r,0,Math.PI*2);x.fill()});x.shadowBlur=0;requestAnimationFrame(draw)}resize();addEventListener('resize',resize);draw()})();
